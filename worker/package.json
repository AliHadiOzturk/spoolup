{
  "name": "spoolup-landing-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SpoolUp landing site",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.20231218.0"
  }
}
